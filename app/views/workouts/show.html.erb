<p>
    <strong>Workout Name </strong>
    <%= @workout.workout %>
</p>
<p>
    <strong>Description </strong>
    <%= @workout.description %>
</p>
<p>
    <strong>Workout Date </strong>
    <%= @workout.date %>
    
    
<h2>Notes</h2>
<% @workout.comments.each do |comment| %>
 
  <p>
    <strong>Note:</strong>
    <%= comment.body %>
  </p>
<% end %>
<h4>Add a note:</h4>
<%= form_for([@workout, @workout.comments.build]) do |f| %>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>


</p>
<%= link_to 'Add another workout ', new_workout_path, class: 'btn btn-primary btn-xs' %>
<br>
<%= link_to 'Workouts Page ', workouts_path, class: 'btn btn-primary btn-xs' %>
<br>
<%= link_to 'Home Page ', welcome_index_url, class: 'btn btn-primary btn-xs' %>